import {Request, Response} from "express";

export const createUserSessionHandler = (req:Request, res:Response)=>{

    //Validate the user's password

    //Create a session

    //Create an access token

    //Create a refresh token
}